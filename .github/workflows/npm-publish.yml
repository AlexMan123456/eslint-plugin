name: Publish to NPM Registry

permissions:
  id-token: write

on:
  push:
    branches:
      - main

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/eslint-plugin' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@5144246069b920323a6a0719e22140a43fd3bdb6 # v3.1.1
